#include <iostream>
#include <cmath>
#include <cstdlib>
#include "solarsystem.h"
using namespace std;

int main (int numArguments, char ** arguments){
  int numTimesteps = 1000;
  if (numArguments >= 2) numTimesteps = atoi(arguments[1])

  Solarsystem Solarsystem; //Name of class

  CelestrialBody &Sun = solarsystem.CreateBody(pos, vec, mass)








}
